VITE_API_URL=http://localhost:8080
# AI chat is proxied through the Spring Boot backend — no direct frontend AI key needed.
# VITE_AI_URL is reserved for future direct-client integrations (currently unused).
VITE_AI_URL=
